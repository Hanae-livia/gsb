$(document).ready(function() {
    // Gestion de la classe "has-error"
    //for (var i = 1; i < 2; i++) {
    //    $('#product_id').on('change', function () {
    //    $(this).after('<select class="form-control" name="product_ids[]" id="product_id"><option selected value="">Selectionner un produit</option>{% for product in products %}<option value="{{ product.reference }}">{{ product.nom_commercial }}</option>{% endfor %}</select>');
    //    });
    //}
});
